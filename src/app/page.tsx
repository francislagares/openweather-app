import Navbar from '@/components/Navbar';

const Home = () => {
  return (
    <main className='flex min-h-screen flex-col gap-4 bg-gray-100'>
      <Navbar />
    </main>
  );
};

export default Home;
